<div class="col-md-3 col-sm-6 col-12">
    <?php
    $box=new LTE\Infobox;
    $box->icon('fa fa-flag-o');
    $box->bg('info-gradient');
    $box->text('Coffee');
    $box->number(410);
    $box->progress(45);
    $box->progress_description('Envie de chier');
    echo $box;
    ?>
</div>
<!-- /.col -->

<div class="col-md-3 col-sm-6 col-12">
    <?php
    $box=new LTE\Infobox;
    $box->icon('fa fa-flag-o');
    $box->bg('success-gradient');
    $box->text('Coffee');
    $box->number(410);
    $box->progress(45);
    $box->progress_description('Envie de chier');
    echo $box;
    ?>
</div>
<!-- /.col -->

<div class="col-md-3 col-sm-6 col-12">
    <?php
    $box=new LTE\Infobox;
    $box->icon('fa fa-flag-o');
    $box->bg('warning-gradient');
    $box->text('Coffee');
    $box->number(410);
    $box->progress(45);
    $box->progress_description('Envie de chier');
    echo $box;
    ?>
</div>
<!-- /.col -->

<div class="col-md-3 col-sm-6 col-12">
    <?php
    $box=new LTE\Infobox;
    $box->icon('fa fa-flag-o');
    $box->bg('danger-gradient');
    $box->text('Coffee');
    $box->number(410);
    $box->progress(45);
    $box->progress_description('Envie de chier');
    echo $box;
    ?>
</div>
<!-- /.col -->