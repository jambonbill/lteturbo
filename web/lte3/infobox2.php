<div class="col-md-3 col-sm-6 col-12">
    <?php
    $box=new LTE\Infobox;
    $box->icon('fa fa-flag-o');
    $box->bg('info');
    $box->text('Coffee');
    $box->number(410);
    $box->progress(95);
    $box->progress_description('Envie de chier');
    echo $box;
    ?>
</div>

<div class="col-md-3 col-sm-6 col-12">
    <?php
    $box=new LTE\Infobox;
    $box->icon('fa fa-flag-o');
    //$box->type('danger');
    $box->bg('success');
    $box->text('Coffee');
    $box->number(410);
    $box->progress(95);
    $box->progress_description('Envie de chier');
    echo $box;
    ?>
</div>

<div class="col-md-3 col-sm-6 col-12">
    <?php
    $box=new LTE\Infobox;
    $box->icon('fa fa-flag-o');
    $box->bg('warning');
    $box->text('Coffee');
    $box->number(410);
    $box->progress(45);
    $box->progress_description('Envie de chier');
    echo $box;
    ?>
</div>

<div class="col-md-3 col-sm-6 col-12">
    <?php
    $box=new LTE\Infobox;
    $box->icon('fa fa-flag-o');
    $box->bg('danger');
    $box->text('Coffee');
    $box->number(410);
    $box->progress(95);
    $box->progress_description('Envie de chier');
    echo $box;
    ?>
</div>
